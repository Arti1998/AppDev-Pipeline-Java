@Library('github.com/ranjitaws2020/java-shared-library@pipeline') _

pipeline {
    agent any
    
    stages {  
        stage('Provision Infrastructure') {
            steps {
                bat label: '', script: """
                terraform -chdir=infra init -input=false
                terraform -chdir=infra plan -out=tfplan -input=false
                terraform -chdir=infra apply -input=false tfplan   
                """
            }
        }
        stage('Pull the file off Nexus') {
            steps {
                DownloadFromNexus()                  
            }
        }
        stage('Deploy Artifact') {
            steps {
                   withCredentials([sshUserPrivateKey(credentialsId: 'my-ssh-credentials', keyFileVariable: 'SSH_KEY', usernameVariable: 'SSH_USER')]) {
                   sshagent(['my-ssh-credentials']) {
                     sh """
                         ssh -o StrictHostKeyChecking=no -i ${SSH_KEY} ${SSH_USER}@${SSH_HOST} 'docker build -t javasampletomcat:latest .'
                         ssh -o StrictHostKeyChecking=no -i ${SSH_KEY} ${SSH_USER}@${SSH_HOST} 'docker save javasampletomcat:latest | docker load'
                         ssh -o StrictHostKeyChecking=no -i ${SSH_KEY} ${SSH_USER}@${SSH_HOST} 'docker run -d --name HelloWorld -p 8280:80 javasampletomcat:latest'
                     """
                      }
                    }
               }
          }
    }
}
